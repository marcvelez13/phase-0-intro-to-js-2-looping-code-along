const names = (['value1','value2','value3']);
function writeCards(names,){
    let i = 0; i++;
    while(names < names.legth){
    console.log(`Thank you, ${names[i]}, for the wonderful suprise gift! `);
    debugger;
}
return writeCards;
}

function countdown(){
     const countdown = 0
    let i = 0; i--;
    while(i <= 10);
    console.log(countdown[i]);
}